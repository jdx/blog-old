- content_for :title do
  %title Blog by Jeff Dickey

%h1 Blog
#body
  - if @tag
    %h2 Posts tagged with "#{@tag}"
  #posts
    - @post_months.each do |month|
      .date-group
        .date-label
          .month= month[0].strftime("%b")
          .year= month[0].year
        %ul
          - for post in month[1]
            %li
              - if @post.image.url
                = image_tag @post.image.url(:small_thumb)
              %a{ 'href' => post.permalink }= post
